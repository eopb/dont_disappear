
image: "rust:latest"

stages:
  - check
  - test
  - publish
  - post-run

cache:
  key: "${CI_JOB_NAME}"
  paths:
    - ${CI_PROJECT_DIR}/target/

clean-code:
  stage: check
  script:
    - rustup component add rustfmt-preview
    - cargo fmt --all -- --check
  allow_failure: true


check-dont_disappear:
  stage: check
  script:
    -  cargo check


unittests:
  stage: test
  script:
    - apt-get update
    - apt-get install tree -y
    - tree
    - cargo build
    - cd examples
    - rm -f any_key_to_continue.rs
    - cd ..
    - tree
    - cargo test

benchmarktests:
  stage: test
  script:
    - cargo bench


docs:
  stage: publish
  cache: {}
  script:
    - cargo doc --no-deps
    - rm -r -f docs
    - mkdir docs
    - cp -R target/doc docs
  when: on_success
  artifacts:
    paths:
      - docs
    expire_in: 2 weeks
  only:
    refs:
      - master
      - dev

cratesio:
  stage: publish
  cache: {}
  script:
    - cargo login $CARGO_LOGIN
    - cargo package
    - cargo publish
  when: on_success
  only:
    refs:
      - master
  allow_failure: true


clippy:
  stage: post-run
  script:
    - rustup default nightly
    - rustup update
    - rustup component add clippy-preview
    - cargo clippy --all -- -D clippy::all
  allow_failure: true
